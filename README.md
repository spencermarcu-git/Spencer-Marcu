# @SpencerMarcu
American singer-songwriter Spencer Marcu 

## Development

If you're setting up a local clone of this repository, run the following commands to align your local branch with the remote `SpencerMarcu` branch:

```sh
git branch -m main SpencerMarcu
git fetch origin
git branch -u origin/SpencerMarcu SpencerMarcu
git remote set-head origin -a
git push -u origin SpencerMarcu
```
